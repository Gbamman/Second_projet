{% extends 'confsociete/template.html.twig' %}

{% block breadcrumb %}
    <div class="container">
        <ol class="breadcrumb">
            <li> {{'menu.dashboard.admin'|trans(arguments = {}, domain = 'menu')}} </li>
            <li class="active"> {{'societe.name'|trans(arguments = {}, domain = 'societe')}}  </li>
        </ol>
    </div>
{% endblock %}

{% block content %}
<div class="panel panel-default">
    <div class="panel-heading main-color-bg">
        <h3 class="panel-title">{{'societe.indexview.title'|trans(arguments = {}, domain = 'societe')}} </h3>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-3">
                <a type="button" href="{{ path('societe_new') }}" class="btn main-new-action-bg"> <span class="glyphicon glyphicon-plus-sign"></span> <b> {{'societe.indexview.addbtnlabel'|trans(arguments = {}, domain = 'societe')}} </b></a>
            </div>
        </div>
        <br>
        <div class="row">
            <div class="col-md-12">
                <input class="form-control" type="text" id="form_index_search_societe"  placeholder="{{'societe.indexview.table.searchformlabel'|trans(arguments = {}, domain = 'societe')}}">
            </div>
        </div>
        <br>

        <div id="loading_search" style="text-align: center;visibility: hidden">
            <span><i class="glyphicon glyphicon-refresh" ></i> Veuilez patienter pendant le chargement des données ... </span>
        </div>

        <div id="div_tablo_liste_societe">
            {% include 'confsociete/include/tableau.html.twig' with  {'confSocietes': confSocietes} %}
        </div>




    {#<ul>
        <li>
            <a href="{{ path('societe_new') }}">Create a new confSociete</a>
        </li>
    </ul>#}
    </div>
    </div>

{% endblock %}
