{% extends 'confschemaoperation/template.html.twig' %}

{% block breadcrumb %}
    <div class="container">
        <ol class="breadcrumb">
            <li> {{'menu.dashboard.param'|trans(arguments = {}, domain = 'menu')}} </li>
            <li class="active"> {{'schemaoperation.name'|trans(arguments = {}, domain = 'schemaoperation')}}  </li>
            <li class="active"> {{'schemaoperation.editview.title'|trans(arguments = {}, domain = 'schemaoperation')}}</li>
        </ol>
    </div>
{% endblock %}

{% block content %}
<div class="panel panel-default">
    <div class="panel-heading main-color-bg">
        <h3 class="panel-title">{{'schemaoperation.editview.title'|trans(arguments = {}, domain = 'schemaoperation')}}</h3>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-3">
                <a type="button" href="{{ path('schemaoperation_index') }}" class="btn main-new-action-bg"> <span class="glyphicon glyphicon-arrow-left"></span> <b>   {{'crud.backbtnlabel'|trans(arguments = {}, domain = 'crud')}}  </b></a>
            </div>
        </div>

        <br>
        <div class="well">
            {{ form_start(edit_form) }}
            <div class="row">
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="control-label"  for="configbundle_confschemaoperation_codeSchema">{{'schemaoperation.newview.form.labelcodeschema'|trans(arguments = {}, domain = 'schemaoperation')}}  </label>
                        {{ form_widget(edit_form.codeSchema, {'attr' : {'class' : 'form-control'}}) }}
                        <span class="text-danger">{{ form_errors(edit_form.codeSchema) }}</span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="control-label"  for="configbundle_confschemaoperation_nbrEtape">{{'schemaoperation.newview.form.labelnbretape'|trans(arguments = {}, domain = 'schemaoperation')}}  </label>
                        {{ form_widget(edit_form.nbrEtape, {'attr' : {'class' : 'form-control'}}) }}
                        <span class="text-danger">{{ form_errors(edit_form.nbrEtape) }}</span>
                    </div>
                </div>
                <div class="col-md-4">
                </div>
            </div>
            <div class="row">
                <div class="col-md-4">
                   <div class="form-group">
                        <label class="control-label"  for="configbundle_confschemaoperation_societe">{{'schemaoperation.newview.form.labelsociete'|trans(arguments = {}, domain = 'schemaoperation')}}  </label>
                        {{ form_widget(edit_form.societe, {'attr' : {'class' : 'form-control'}}) }}
                        <span class="text-danger">{{ form_errors(edit_form.societe) }}</span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="control-label"  for="configbundle_confschemaoperation_journal">{{'schemaoperation.newview.form.labeljournal'|trans(arguments = {}, domain = 'schemaoperation')}}  </label>
                        {{ form_widget(edit_form.journal, {'attr' : {'class' : 'form-control'}}) }}
                        <span class="text-danger">{{ form_errors(edit_form.journal) }}</span>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="control-label"  for="configbundle_confschemaoperation_typeOperation">{{'schemaoperation.newview.form.labeltypeoperation'|trans(arguments = {}, domain = 'schemaoperation')}}  </label>
                        {{ form_widget(edit_form.typeOperation, {'attr' : {'class' : 'form-control'}}) }}
                        <span class="text-danger">{{ form_errors(edit_form.typeOperation) }}</span>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="form-group">
                        <label class="control-label"  for="configbundle_confschemaoperation_designation">{{'schemaoperation.newview.form.labeldesignation'|trans(arguments = {}, domain = 'schemaoperation')}}  </label>
                        {{ form_widget(edit_form.designation, {'attr' : {'class' : 'form-control'}}) }}
                        <span class="text-danger">{{ form_errors(edit_form.designation) }}</span>
                    </div>
                </div>
            </div>
            {#{ form_widget(form) }#}
            {{ form_rest(edit_form) }}

            <button class="btn btn-success"  type="submit" > <span class="glyphicon  glyphicon-saved"></span> {{'crud.editbtnlabel'|trans(arguments = {}, domain = 'crud')}} </button>
            {#<input  type="submit" value="Create" />#}
            {{ form_end(edit_form) }}
        </div>
    </div>
</div>
{% endblock %}
