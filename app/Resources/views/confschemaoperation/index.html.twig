{% extends 'confschemaoperation/template.html.twig' %}
{% block breadcrumb %}
    <div class="container">
        <ol class="breadcrumb">
            <li> {{'menu.dashboard.param'|trans(arguments = {}, domain = 'menu')}} </li>
            <li class="active"> {{'schemaoperation.name'|trans(arguments = {}, domain = 'schemaoperation')}}  </li>
          </ol>
    </div>
{% endblock %}
{% block content %}
<div class="panel panel-default">
    <div class="panel-heading main-color-bg">
        <h3 class="panel-title">{{'schemaoperation.indexview.title'|trans(arguments = {}, domain = 'schemaoperation')}}</h3>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-3">
                <a type="button" href="{{ path('schemaoperation_new') }}" class="btn main-new-action-bg"> <span class="glyphicon glyphicon-plus-sign"></span> <b> {{'schemaoperation.indexview.addbtnlabel'|trans(arguments = {}, domain = 'schemaoperation')}}</b></a>
            </div>
        </div>
        <br>
        <div class="row">
            <div class="col-md-12">
                <input class="form-control" type="text" placeholder="{{'schemaoperation.indexview.table.searchformlabel'|trans(arguments = {}, domain = 'schemaoperation')}}">
            </div>
        </div>
        <br>
        <!--<h1>Liste des compte comptables plans </h1> -->
         <div id="div_tableau_liste_schema">
            {% include 'confschemaoperation/include/tableau.html.twig' with  {'confSchemaOperations': confSchemaOperations} %}
        </div>
      <!--<ul>
       <li>
           <a href="{#{ path('confschemaoperation_new') }#}">Create a new confschemaoperation</a>
       </li>
   </ul> -->
    </div>
    </div>

{% endblock %}

