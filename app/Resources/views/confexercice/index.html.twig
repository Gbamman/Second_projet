{% extends 'confexercice/template.html.twig' %}

{% block breadcrumb %}
    <div class="container">
        <ol class="breadcrumb">
            <li> {{'menu.dashboard.param'|trans(arguments = {}, domain = 'menu')}} </li>
            <li class="active"> {{'exercice.name'|trans(arguments = {}, domain = 'exercice')}}  </li>
          </ol>
    </div>
{% endblock %}
{% block content %}
<div class="panel panel-default">
    <div class="panel-heading main-color-bg">
        <h3 class="panel-title">{{'exercice.indexview.title'|trans(arguments = {}, domain = 'exercice')}}</h3>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-3">
                <a type="button" href="{{ path('exercice_new') }}" class="btn main-new-action-bg"> <span class="glyphicon glyphicon-plus-sign"></span> <b> {{'exercice.indexview.addbtnlabel'|trans(arguments = {}, domain = 'exercice')}}</b></a>
            </div>
        </div>
        <br>
        <div class="row">
            <div class="col-md-12">
                <input class="form-control" type="text" placeholder="{{'exercice.indexview.table.searchformlabel'|trans(arguments = {}, domain = 'exercice')}}">
            </div>
        </div>
        <br>
       <!--<h1>Liste des compte comptables plans </h1> -->
       <div id="div_tableau_liste_exercice">
            {% include 'confexercice/include/tableau.html.twig' with  {'confExercices': confExercices} %}
        </div>
         <!--  <ul>
       <li>
           <a href="{#{ path('confexercice_new') }#}">Create a new confexercice</a>
       </li>
   </ul> -->
    </div>
    </div>

{% endblock %}